/// <reference types="@cloudflare/workers-types" />
import type { PulseResultType } from "./types.js";
/**
 * Represents the subset of the Prisma DMMF that is needed to unmarshall Pulse
 * events.
 */
export interface ModelDMMF {
    dbName: string | undefined;
    fields: Array<{
        dbName: string | undefined;
        isList: boolean;
        kind: string;
        name: string;
        type: string;
    }>;
}
export interface StreamArgs {
    name: string | undefined;
}
export interface PulseSubscriptionInit {
    endpoint: URL;
    apiKey: string;
    model: ModelDMMF;
    modelName: string;
    where: unknown;
    stream: StreamArgs;
}
export declare class PulseSubscription<T extends PulseResultType<any, any>> implements AsyncIterable<T> {
    #private;
    constructor(init: PulseSubscriptionInit);
    get open(): Promise<void>;
    [Symbol.asyncIterator](): AsyncIterator<T, void, void>;
    acknowledgeEvent(id: string): Promise<void>;
    stop(): void;
}
