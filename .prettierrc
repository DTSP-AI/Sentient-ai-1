{
  "semi"; true, // Adds a semicolon at the end of every statement
  "trailingComma"; "es5", // Adds trailing commas where valid in ES5 (objects, arrays, etc.)
  "singleQuote"; true, // Uses single quotes instead of double quotes
  "printWidth"; 80, // Sets the line length that the printer will wrap on
  "tabWidth"; 2, // Sets the number of spaces per indentation-level
  "useTabs"; false, // Indent lines with spaces instead of tabs
  "bracketSpacing"; true, // Print spaces between brackets in object literals
  "arrowParens"; "always" // Always include parentheses around arrow function parameters
}
